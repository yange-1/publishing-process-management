# 校了么 —— 测试与验收

版本：V1.0
日期：2026年8月26日
阶段：返稿配送与品牌化收尾验收

---

## 1. 自动测试结果

以本地真实测试结果为准：

- **全量自动测试：498 / 498 通过**，0 失败、0 跳过。
- `npm run lint`：通过（无错误、无警告）。
- `npm run build`：通过（TypeScript 通过，16 个页面正常生成）。

测试覆盖新增的配送业务（`test/delivery.test.ts`）、角色差异化页面文案（`test/copy.test.ts`）与品牌名称（`test/brand.test.ts`），并保留原有任务、权限、统计、报表、配送等全部测试继续通过。

---

## 2. 数据库完整性检查

- 数据库完整性检查通过（`integrity_check` 与 `quick_check` 均 ok）。
- 演示数据库原有业务数据未减少。
- 正式数据库未触碰（全程只读核验，未写入任何数据）。

---

## 3. 人工验收记录

1. 外校主管看到本公司「运送中」。
2. 外校主管点击「送达」后稿件从「运送中」消失。
3. 对应责任编辑看到已送达稿件（出现在「已“出餐”」版块）。
4. 其他责任编辑看不到该稿件（只能看到本人负责的配送稿件）。
5. 责任编辑点击确认「收货」后，该稿件立即从首页消失。
6. Dominance 代送达必须填写代操作原因。
7. A、B 公司仓库筛选数量分别联动（全部/A/B/清除）。
8. 平台名称在登录页、首页和浏览器标签均显示为「校了么」。
9. 最终人工核验完毕，功能完整。

---

## 4. 结论

- 返稿配送（运送中 / 送达 / 已送达确认收货）与品牌化（校了么）功能完整。
- 自动测试 498 项、lint、build、数据库完整性检查全部通过，演示数据未减少，正式库未触碰。
- 本项目尚未提交、尚未推送，也未部署到正式环境。
