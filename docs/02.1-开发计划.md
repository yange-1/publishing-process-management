---

# 校了么 - 开发计划

## 1. 项目概述

### 1.1 项目名称

校了么

### 1.2 项目目标

建设一个面向内部出版业务的小型项目管理平台，实现出版项目的信息统一管理、流程节点可视化展示以及项目进度跟踪。

通过系统替代目前依靠人工询问、微信群沟通等方式获取项目进度的工作模式，提高出版流程管理效率。

### 1.3 项目定位

本项目第一阶段定位为：

> 轻量化内部业务管理工具（MVP版本）

不追求复杂企业级项目管理功能，而优先解决当前出版流程中的核心管理问题。

后续根据实际使用情况逐步扩展。

---

# 2. 开发原则

项目开发遵循以下原则：

## 2.1 优先解决核心业务问题

第一阶段重点实现：

- 出版项目统一管理；
- 流程阶段展示；
- 当前状态查看；
- 滞留项目提醒。

暂不开发复杂功能：

- 企业级权限体系；
- 在线协同编辑；
- 复杂统计分析；
- 全流程数字出版系统。

---

## 2.2 保持系统简单稳定

系统面向内部工作人员使用，因此：

- 操作流程应简单；
- 页面信息应清晰；
- 减少学习成本；
- 保证数据安全。

---

## 2.3 保留未来扩展能力

系统设计时预留：

- 数据统计分析；
- 多用户权限管理；
- 移动端访问；
- 与出版社其他系统对接能力。

---

# 3. 开发阶段规划

## 阶段一：项目基础搭建

### 目标

完成项目开发环境和基础框架建设。

### 工作内容

完成：

- Next.js项目初始化；
- React基础配置；
- TypeScript配置；
- Tailwind CSS配置；
- 项目目录规划；
- 基础页面结构。

### 输出成果

形成：

- 可运行的Web项目；
- 基础代码框架；
- 项目文档体系。

---

# 阶段二：基础页面开发

## 目标

完成系统基本操作界面。

## 工作内容

### 1. 首页/项目列表页面

实现：

- 项目列表展示；
- 项目基本信息显示。

项目主要信息：

- 书名；
- 责任编辑；
- 校对负责人；
- 当前阶段；
- 当前状态。

首页按角色差异化展示：外校公司主管首页直接展开“待确认收稿”区域（仅本公司）并可逐条确认，其“已完成任务”点击进入独立分页页面；责任编辑首页按本人维度展示（待确认/生产线/已完成仅本人书稿，部门书稿仓库展示全部待开始并支持按外校公司筛选与数量显示）；其余角色按书稿仓库—生产线—已完成—总控预警展示。

---

### 2. 任务发布功能

由责任编辑发布本校次校对任务（即“来稿”），系统自动生成来稿时间，任务进入书稿仓库。

录入或确认：

- 书名；
- 责任编辑（发布人）；
- 稿件等级；
- 本次流程阶段；
- 工作字数（本次校对任务字数，正整数；按每一次校对任务分别记录）。

---

### 3. 项目编辑功能

支持修改：

- 项目信息；
- 当前流程节点；
- 项目状态。

---

### 4. 取消任务功能

支持取消错误任务（不物理删除，保留历史）。

---

# 阶段三：出版流程管理功能（核心功能）

## 目标

实现出版流程节点可视化。

## 工作内容

建立出版流程节点模型：

包括：

- 初审；
- 一校；
- 二校；
- 三校；
- 加校。

（核红是「本次工作内容」之一，不是校次；工作内容为读校 / 核红 / 读校且核红。）

每个校次通过四个动作推进，分别由不同角色发起：

- 发布（社内责任编辑）：任务进入待确认收稿，状态为“待确认收稿”，生成来稿时间；
- 确认收稿（外校公司主管）：一键确认，状态变为“待开始”，记录确认人与确认时间；确认时页面显示工作字数，可修改外校确认字数（默认等于工作字数）后再确认；
- 开始（空闲校对人员）：一键开始，系统自动设为校对负责人，任务进入生产线，记录开始时间；
- 结束（当前校对人员）：一键结束，任务完成并退出生产线，记录结束时间。

---

## 流程特点设计

### 1. 支持不同流程

不是所有书稿必须经历全部流程。

例如：

普通图书：

```
初审 → 一校 → 二校 → 核红
```

简单项目：

```
初审
```

特殊项目：

```
一校 → 核红
```

系统支持根据实际情况选择流程节点。

---

### 2. 节点记录

每个节点记录：

- 是否完成；
- 开始时间；
- 完成时间。

---

### 3. 当前状态显示

系统显示：

- 当前完成节点；
- 当前进行节点；
- 下一工作节点。

---

# 阶段四：项目状态管理与提醒

## 目标

及时发现滞留项目。

## 工作内容

任务状态：

包括：

- 待确认收稿；
- 待开始；
- 进行中；
- 已结束；
- 已取消（超级管理员纠错）。

---

## 阈值管理

第一阶段采用简单阈值管理。

不同流程节点设置不同预警时间。

例如：

### 初审

默认：

90天

### 其他校次

普通稿：

30天

急稿：

15天

重要急稿：

7天

---

## 滞留提醒

当项目超过设定阈值：

系统标记：

“滞留”

并提醒：

- 校对管理人员；
- 项目负责人。

---

# 阶段五：数据保存与安全

## 目标

保证业务数据可靠保存。

## 第一阶段方案

本次已完成最终业务规则与数据模型统一（users / companies / books / tasks / task_events / audit_log 六张表），下一步进入第3天 SQLite 建库与流程实施。

采用：

本地数据库存储（SQLite）。

要求：

- 数据不丢失；
- 支持备份；
- 数据结构方便未来升级。

---

## 后续扩展方向

根据使用规模，可升级：

- SQLite；
- PostgreSQL；
- 云数据库。

实现：

- 多人访问；
- 权限控制；
- 数据同步。

---

# 阶段六：统计分析功能（扩展）

## 目标

利用系统积累的数据辅助管理决策。

未来支持：

## 出版统计

例如：

- 年出版数量；
- 月出版数量；
- 各责任编辑项目数量。

---

## 流程效率分析

例如：

统计：

- 平均初审时间；
- 平均一校时间；
- 平均二校时间；
- 不同类型书稿流程耗时。

---

## 项目风险分析

例如：

发现：

- 哪些节点最容易滞留；
- 哪些项目长期滞留。

---

# 阶段七：系统扩展方向

未来可向数字化出版管理平台发展。

可能扩展：

## 1. 权限管理

角色（开放查看，角色限制操作）：

- 社内责任编辑：发布任务、取消自己未开始的任务、查看和搜索全部书稿；
- 外校公司主管：确认收稿、查看全部书稿；
- 校对人员：开始待开始任务、结束自己的进行中任务、查看全部书稿；
- 社内校对主管：超级管理员，可执行全部操作、代操作、纠错、管理账号（覆盖外校公司）。

正式多人使用前，简单登录和角色权限是内部试用版的必要功能，演示版可使用虚构数据和静态角色展示。

---

## 2. 在线稿件管理

包括：

- 稿件上传；
- 在线查看；
- 版本管理。

---

## 3. 流程线上化

实现：

- 节点自动流转；
- 工作提醒；
- 审批记录。

---

## 4. 与其他系统对接

未来可以考虑：

- 出版ERP；
- OA系统；
- 财务系统；
- 数字出版平台。

---

# 4. 第一阶段预计交付成果

完成MVP版本后，实现：

## 功能成果

✅ 项目列表管理

✅ 项目创建、修改、删除

✅ 出版流程展示

✅ 当前节点查看

✅ 项目状态管理

✅ 滞留提醒

✅ 基础数据保存

---

## 文档成果

完成：

- 项目需求文档；
- 技术方案；
- 开发过程记录；
- 测试验收记录；
- 使用说明。

---

# 5. 开发方式

采用：

AI辅助开发模式。

工具：

- Visual Studio Code
- Claude Code

开发流程：

1. 明确需求；
2. 制定方案；
3. AI辅助生成代码；
4. 人工测试调整；
5. 持续完善。

---

# 6. 后续优化方向

MVP版本稳定运行后，根据实际使用反馈逐步增加：

- 更完善提醒机制；
- 数据统计；
- 权限控制；
- 移动端支持；
- 数字出版流程整合。

---

## 阶段六（第一阶段已实现部分）：统计报表中心

第一阶段已实现网页版统计报表（月报 / 半年报 / 年报，无周报），访问路径 `/reports`：

- 权限：仅社内校对主管（INTERNAL_ADMIN，全平台）与外校公司主管（EXTERNAL_SUPERVISOR，仅本公司）可访问；责任编辑、校对人员不可访问；公司范围由服务端按登录用户强制确定。
- 统计口径：每一项校对任务（每一次校次）计为 1「本次」，不按书名去重；取消任务不计入。
- 五项指标：来稿量（本次 + 工作字数）、完成量（本次 + 外校确认字数）、按期返回量、滞留量、平均50万字完成周期（天）。
- 平均50万字完成周期公式：已完成任务总耗时 ÷ 外校确认总字数 × 500000。
- 预测版（期间未结束时按平滑法预测，标注「预测」）与实绩版（期间结束后真实数据）。
- 字数缺失不补 0、不虚构。Excel 导出与更深入的流程效率 / 工作量分析留待后续阶段。

## 阶段七（已完成）：配送体验与品牌化优化

在四步校对闭环之后补齐返稿配送体验，并将平台正式名称统一为「校了么」。本阶段已完成，不虚构日期或工时。

完成内容：

1. 配送事实层：新增 deliveries（送达）与 delivery_receipts（确认收货）两张只追加事实表；「运送中」「已送达」「确认收货」不改变 COMPLETED 任务状态，不进入 tasks.status 枚举。
2. 外校主管「运送中」列表与「送达」按钮；Dominance「代送达」（必须填写代操作原因并写审计）。
3. 责任编辑右侧「已“出餐”」版块：已送达未确认（含确认“收货”按钮）与配送中；已送达未确认按上海自然日最多显示 7 天。
4. 责任编辑书稿仓库统计卡与列表标题按公司筛选联动显示「前方还有 x 份待制作」；生产线标题改为「“备餐”中，请耐心等待～」。
5. 品牌名统一为「校了么」（登录页、首页顶部、浏览器标签 metadata title）。
6. 页面外卖化文案统一使用中文双引号“ ”与中文全角波浪号“～”。
7. 自动测试扩充至 498 项，lint 与 build 均通过。

## 文档版本

版本：V1.0

日期：2026年8月18日

状态：

开发规划阶段

---
