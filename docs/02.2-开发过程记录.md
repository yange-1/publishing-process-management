# 需求确认记录：确定项目优先级和滞留判断规则

## 时间

2026年8月19日

## 背景

在进一步梳理实际校对工作流程后，明确系统第一阶段重点不是统计每个校次耗时，而是帮助校对管理人员快速发现：

- 哪些书稿正在处理；
- 哪些书稿等待时间较长；
- 哪些重要项目需要优先关注。

---

## 确定方案

### 1. 计时起点

项目处理周期从：

校对部门收到书稿时间

开始计算。

---

### 2. 阈值规则

根据稿件重要程度设置：

普通稿件：

30天

急稿：

15天

重要急稿：

7天

初审：

90天

---

### 3. 首页展示方案

采用卡片式流程看板。

展示：

- 书名；
- 当前阶段；
- 负责人；
- 稿件等级；
- 时间。

---

### 4. 排序规则

第一排序：

稿件等级。

第二排序：

收到书稿时间。

重要急稿和急稿通过星标突出显示。

---

## 后续扩展

未来版本可增加：

- 各校次独立时间记录；
- 平均处理时间统计；
- 流程效率分析。

---

# 需求确认记录：确定编辑发布、主管分发和校对人员结束的任务责任链（旧方案，现已废止）

## 时间

2026年8月20日

## 确定方案

1. 来稿等于责任编辑发布任务：责任编辑在系统中发布一条校对任务，即代表该校次书稿进入校对部门。
2. 来稿时间由系统自动生成：任务发布时间、来稿时间、滞留计时起点、进入书稿仓库时间为同一个系统时间点。
3. 校对主管不录入来稿：来稿不由校对主管填写，来稿时间由系统自动生成，不重复录入。
4. 校对主管负责分发：从书稿仓库选择任务和空闲校对人员，执行“分发并开始”，记录分发人、分发时间、进入生产线时间和节点开始时间。
5. 校对人员负责结束：完成本校次后点击“结束校对”，记录结束操作人、结束时间和本校次完成时间。
6. 每一步保存操作人和时间：发布、分发、结束三个动作分别记录发起人和时间。
7. 新任务成为校对主管的页面提醒：发布后校对主管首页书稿仓库数量自动增加，页面显示“新任务”提醒。
8. 一个校对人员同一时段只能存在一条进行中的校对任务，已占用时阻止再次分发。
9. 下一校次由责任编辑重新发布新任务，新任务重新进入书稿仓库并生成新的来稿时间。

---

# 需求确认记录：确定四步极简流程、开放查看与自动责任记录

## 时间

2026年8月20日

## 确定方案

1. 四步极简流程：社内责任编辑“发布校对任务” → 外校公司主管一键“确认收稿” → 空闲校对人员一键“开始校对” → 当前校对人员一键“结束校对”。无分发、无领取登记、无返回责任编辑。
2. 来稿由系统生成：来稿 = 责任编辑发布任务，发布时间 = 来稿时间 = 滞留计时起点，由系统自动填写。
3. 外校公司主管不录入领取时间、不分发任务，仅一键确认收稿。
4. 校对人员自行开始：对“待开始”任务点击“开始校对”，系统自动设为校对负责人并记录开始时间；一名校对人员同一时段只能加工一本书。
5. 开放查看：所有登录用户可查看和搜索全部书稿，角色主要限制操作权限，不限制查看范围。
6. 状态统一：待确认收稿 / 待开始 / 进行中 / 已结束 / 已取消。
7. 自动填写：操作账号、操作人、所属公司、操作时间、状态、负责人、状态变化均由系统自动生成。
8. 事件只追加：TASK_PUBLISHED / RECEIPT_CONFIRMED / TASK_STARTED / TASK_COMPLETED / TASK_CANCELLED。
9. 数据对象：users / companies / books / tasks / task_events / audit_log；tasks 保留 cancelled_by、cancelled_at、cancellation_reason。
10. 社内校对主管为超级管理员，覆盖外校公司及其账号，代操作与纠错进入审计日志。
11. 取消：编辑可取消自己未开始的任务，超级管理员可纠错取消；取消记录取消人、时间、原因。

---

# 开发记录：SQLite 数据层已建立

## 时间

2026年8月20日

## 完成内容

1. SQLite 数据层已建立（better-sqlite3），默认数据库路径 data/publishing-process.db，支持 DATABASE_PATH 环境变量覆盖。
2. 六张核心业务表已建立：companies、users、books、tasks、task_events、audit_log。
3. 外键、状态/角色/校次/星级/事件类型 CHECK 约束、用户登录账号唯一、“一人一书”部分唯一索引均已建立。
4. task_events 与 audit_log 通过触发器实现只追加（禁止 UPDATE / DELETE）。
5. 常用查询索引（状态、发布时间、校对人员、责任编辑、书名、事件 task_id 与时间）已建立。
6. 页面和四步业务接口（发布、确认收稿、开始、结束）尚未连接。

---

# 开发记录：四步流程事务服务已建立

## 时间

2026年8月20日

## 完成内容

1. 四步流程事务服务已建立：发布校对任务、确认收稿、开始校对、结束校对、超级管理员取消，集中在 lib/task-service.ts。
2. 操作人和时间自动生成：普通用户不得提交操作人、负责人或业务时间，均按当前账号与系统时间自动填写。
3. 确认操作幂等：同一任务重复确认不产生重复事件，也不改变已有确认人和确认时间。
4. 开始操作采用原子条件更新（WHERE status='READY_TO_START'），两人同时开始同一任务只允许一人成功。
5. “一人一书”约束已通过部分唯一索引验证，校对人员已有进行中任务时阻止开始第二本。
6. 超级管理员代操作进入审计日志，并记录代操作角色与原因。
7. 页面和登录系统尚未连接。

---

# 开发记录：账号认证基础设施已建立

## 时间

2026年8月20日

## 完成内容

1. 账号认证基础设施已建立：NextAuth（next-auth v4）Credentials 用户名密码登录 + JWT 会话，不新增会话数据表、不引入 ORM。
2. INTERNAL_ADMIN 为最高权限，可进入所有受保护页面并后续执行全部业务操作，最高权限由数据库 role 字段判断，不写死用户名。
3. 登录和业务操作前会回查数据库真实账号，确认账号存在、启用、session_version 一致，并取得真实角色/姓名/公司。
4. 所有新账号默认密码为 123456（仅存哈希，不存明文），首次登录必须修改密码。
5. 修改后的密码至少 6 位，且不强制大小写、数字或特殊符号组合。
6. 密码哈希（bcryptjs）、连续 5 次失败锁定 15 分钟、JWT 会话与 HttpOnly/SameSite 安全 Cookie 已经建立。
7. 初始管理员创建工具（npm run admin:create）已准备，但尚未创建真实管理员。
8. 任务业务按钮和任务 API 尚未连接。

---

# 开发记录：账号管理业务服务已建立

## 时间

2026年8月21日

## 完成内容

1. 账号管理业务服务已建立，集中在 lib/user-admin-service.ts：查看账号列表、创建普通账号、重置密码、停用账号、启用账号，当前阶段不实现永久删除。
2. 账号管理仅限 INTERNAL_ADMIN（已登录、已启用、已完成首次改密），服务回查数据库确认真实角色，不信任调用方传入的角色、姓名或公司。
3. 普通业务账号只允许创建责任编辑、外校主管、校对人员三类，不允许通过普通服务创建 INTERNAL_ADMIN；管理员不能停用自己、不能重置自己的密码、不能操作其他管理员。
4. 新账号默认密码 123456（仅存 bcrypt 哈希）、强制首次改密、失败次数与锁定清零；相同名称+类型的公司自动复用，创建账号与公司记录位于同一事务，失败整体回滚。
5. 重置密码统一为 123456 并强制下次登录改密、session_version +1 使旧会话失效；停用/启用幂等，停用使已有会话失效且不能登录，停用不删除用户与历史记录。
6. 创建、重置、停用、启用四类操作均写入 audit_log（只追加，不可更新/删除），不记录明文密码或密码哈希。
7. 新增 test/user-admin.test.ts（npm run test:users），27 项测试全部通过；认证 24 项、任务 18 项测试回归通过。
8. 正式管理员账号与正式数据库未被测试污染（companies=1、users=1，其余四表为 0）。
9. 账号管理页面和账号管理 API 尚未连接。

---

# 开发记录：账号管理页面已建立

## 时间

2026年8月21日

## 完成内容

1. 账号管理页面已建立（/admin/users），仅已登录、已启用、已完成首次改密的 INTERNAL_ADMIN 可进入；未登录跳登录页、未改密跳改密页、普通角色跳回首页。
2. 首页顶部为 INTERNAL_ADMIN 增加“账号管理”入口，普通角色看不到该入口。
3. 页面支持持续新增责任编辑、外校主管、校对人员（角色下拉仅这三类，不提供超级管理员选项），创建时公司类型按角色自动判断，并复用同名同类型公司。
4. 页面支持按登录账号/姓名搜索、按角色与启用状态筛选、一键清除筛选；每行紧凑展示账号、姓名、角色、公司、状态、密码状态、最近登录、创建时间与操作。
5. 页面支持重置密码、停用、启用；操作人来自服务器会话（Server Actions 每次回查数据库确认真实管理员），不信任浏览器传入的角色或操作人。
6. 所有写操作复用 lib/user-admin-service.ts 并写入 audit_log，业务错误统一转换为安全中文提示，不泄露 SQLite 原始错误。
7. 新增 test/user-admin-page.test.ts（npm run test:users-page），5 项页面层测试全部通过。
8. 正式数据库尚未创建普通人员账号（companies=1、users=1，其余四表为 0），真实管理员未被修改。
9. 下一步：由管理员在页面创建少量代表账号进行人工验收。

---

# 开发记录：发布校对任务页面已建立

## 时间

2026年8月21日

## 完成内容

1. 发布校对任务页面已建立（/tasks/new），首页“发布校对任务”按钮进入该页，仅责任编辑和超级管理员可访问；外校主管、校对人员被服务端拒绝。
2. 支持“新书稿”和“已有书稿继续发起下一校次”两种方式；已有书稿复用原 book_id，普通责任编辑只能选用自己的书稿，超级管理员可选全部。
3. 表单支持校次（初审/一校/二校/三校/加校/核红）、星级（一星普通/二星重要/三星重点评奖紧急）、接收外校公司、备注；仅一家启用外校公司时自动选中。
4. 超级管理员代发布需选择目标责任编辑并填写代发布原因；目标责任编辑采用可输入搜索、也可展开选择的搜索型下拉框（按姓名或登录账号部分匹配），选中后保存真实 editorId。
5. 发布复用并扩展 lib/task-service.ts 的 publishTask 事务服务：校验书稿归属、外校公司、星级、校次、备注长度，初始状态 PENDING_CONFIRMATION，发布时间由系统生成，写入 TASK_PUBLISHED 事件。
6. tasks 表新增“接收外校公司”字段 company_id，并补充幂等迁移（npm run db:migrate）。
7. 新增 test/task-publish.test.ts（npm run test:publish），27 项测试全部通过；页面/账号/认证/任务测试回归通过，lint 与 build 通过。
8. 第一条正式联调任务已创建（初审、待确认收稿）；责任编辑本人发布、管理员代发布字段显示、目标责任编辑搜索下拉均已通过人工验收。
9. 未进入确认收稿阶段。

---

# 开发记录：待确认收稿页面已建立

## 时间

2026年8月21日

## 完成内容

1. 待确认收稿页面已建立（/tasks/pending-confirmation），所有已登录账号可查看；外校主管可一键确认本公司任务，超级管理员可代确认，责任编辑和校对人员只读。
2. 首页增加“待确认收稿（N）”入口，显示当前待确认数量。
3. 外校主管确认时系统自动识别操作账号、所属外校公司与确认时间，不填写领取时间、公司、校对人员或备注，只点击一次，不弹二次确认框。
4. 确认收稿复用并扩展 lib/task-service.ts 的 confirmReceipt 事务服务：原子更新状态为 READY_TO_START，校验主管所属公司与任务 company_id 一致，写入 RECEIPT_CONFIRMED 事件。
5. 超级管理员代确认使用任务已有 company_id，不允许改换接收公司，必须填写原因并写入代理事件与 audit_log。
6. 重复确认幂等（提示“该任务已被确认，无需重复操作”），原子更新保证并发下只有一次成功。
7. 新增 test/task-receipt.test.ts（npm run test:receipt），25 项测试全部通过；其余测试、lint、build 回归通过。
8. 正式数据库唯一任务仍为 PENDING_CONFIRMATION，尚未人工点击确认收稿。
9. 下一步：由外校主管账号亲自点击一次“确认收稿”进行人工验收。

---

# 开发记录：首页接入真实数据库

## 时间

2026年8月21日

## 完成内容

1. 外校主管一键确认收稿已通过人工验收，任务由 PENDING_CONFIRMATION 变为 READY_TO_START。
2. 人工验收发现首页书稿仓库、统计卡仍为演示数据，真实任务未显示。
3. 已建立 lib/dashboard-service.ts 集中只读查询服务，将首页统计、书稿仓库、生产线、已完成、总控预警全部接入真实数据库。
4. 书稿仓库含 PENDING_CONFIRMATION 与 READY_TO_START 两种状态；生产线含 IN_PROGRESS；已完成含 COMPLETED；CANCELLED 不计入当前统计。
5. 滞留按 published_at 起算：初审 90 天、非初审一星 30 天、二星 15 天、三星 7 天；新任务不被误报。
6. 真实任务已正确显示在首页书稿仓库；新增 /tasks/warehouse 完整仓库页，首页仓库与预警最多显示 20 条、超出显示“查看更多”。
7. 新增 test/dashboard.test.ts（npm run test:dashboard），25 项测试全部通过；其余测试、lint、build 回归通过。
8. 未进入开始校对阶段。
9. 人工验收已全部通过：发布、确认收稿、首页五张真实统计卡、书稿仓库显示均正常；正式数据库保留一条联调任务（READY_TO_START）。

---

# 开发记录：开始校对功能已建立

## 时间

2026年8月21日

## 完成内容

1. 开始校对功能已建立：校对人员在书稿仓库对 READY_TO_START 任务点击“开始校对”，状态变为 IN_PROGRESS，任务进入生产线。
2. 复用并扩展 lib/task-service.ts 的 startTask 事务服务：校验校对人员所属公司与任务接收外校公司一致，原子更新状态、写入 proofreader_id 与 started_at，追加 TASK_STARTED 事件。
3. “一人一书”继续由部分唯一索引保证：校对人员已有进行中任务时拒绝开始第二本，两人抢同一任务只有一人成功。
4. 社内超级管理员可代开始：选择目标校对人员（可搜索下拉）并填写原因，写入 is_proxy=1、proxy_role 与审计记录。
5. 首页书稿仓库与 /tasks/warehouse 对 READY_TO_START 任务显示“开始校对”/“代开始”按钮；生产线显示校对人员、开始时间与“我的当前任务”标识。
6. 新增 test/task-start.test.ts（npm run test:start），24 项测试全部通过；其余测试、lint、build 回归通过。
7. 人工验收已通过：校对人员一键开始校对成功，无需填写任何内容，任务从书稿仓库进入生产线，首页统计变为 部门现有书稿 1、书稿仓库 0、生产线 1。
8. 当前联调任务已进入 IN_PROGRESS，校对人员为张萌萌，开始时间由系统生成；正式数据库保留真实联调记录（1 条任务、3 条流程事件、audit_log 仍为 3 条）。

---

# 开发记录：结束校对功能已建立

## 时间

2026年8月21日

## 完成内容

1. 结束校对功能已建立：当前校对人员在生产线对本人 IN_PROGRESS 任务点击“结束校对”，状态变为 COMPLETED，任务进入已完成。
2. 复用并扩展 lib/task-service.ts 的 finishTask 事务服务：原子更新状态为 COMPLETED、写入 finished_at，保留原 proofreader_id 与 started_at，追加 TASK_COMPLETED 事件。
3. 普通校对人员本人结束不写 audit_log；结束后“一人一书”限制自然释放，可开始下一本待开始任务。
4. 社内超级管理员可代结束：填写代操作原因，写 is_proxy=1、proxy_role=PROOFREADER 与审计记录，保留原校对人员。
5. 首页生产线对 IN_PROGRESS 任务显示“结束校对”/“代结束”按钮；已完成区域显示校对人员、开始时间与完成时间。
6. 新增 test/task-finish.test.ts（npm run test:finish），25 项测试全部通过；其余测试、lint、build 回归通过。
7. 人工验收已通过：校对人员张萌萌本人点击“结束校对”，任务由 IN_PROGRESS 变为 COMPLETED，开始时间保留、完成时间由系统自动生成。
8. 首页生产线由 1 变为 0、已完成由 0 变为 1；TASK_COMPLETED 事件成功追加，普通本人操作不新增 audit_log，“一人一书”限制已释放。

---

# 开发记录：书稿搜索与校对历史详情已建立

## 时间

2026年8月21日

## 完成内容

1. 书稿搜索已建立（/search?q=关键词&page=1）：按书名或责任编辑姓名部分匹配，参数绑定、LIKE 通配符转义，每页 20 条，按最近任务发布时间倒序稳定排序。
2. 首页搜索框已连接真实 SQLite 数据（不再使用模拟数据），采用 GET 参数保留关键词。
3. 书稿详情页已建立（/books/[bookId]）：展示基本信息、历次校对任务、流程时间线。
4. 流程时间线从 task_events 读取事件，按 occurred_at 从早到晚排列；管理员代操作显示“代操作”标记、被代操作角色与原因（原因从 audit_log 关联）。
5. 首页仓库/生产线/已完成每行增加“查看历史”链接。
6. 新增 test/history.test.ts（npm run test:history），24 项测试全部通过；其余测试、lint、build 回归通过。
7. 本阶段为只读功能，未修改正式数据库；唯一联调任务保持 COMPLETED。
8. 下一步：由用户在浏览器人工验收搜索与历史详情。

---

# 开发记录：已有书稿继续发起下一校次已建立

## 时间

2026年8月21日

## 完成内容

1. “已有书稿继续发起下一校次”已建立：责任编辑选择已完成的原书稿，系统自动计算下一校次并创建新任务，复用原 books 记录。
2. 服务端 listEligibleBooks 列出可继续发起下一校次的书稿（至少一个已完成、无进行中任务、未到最高校次），下一校次由系统自动计算，不接受浏览器传入校次。
3. publishTask 扩展：已有书稿模式下校验书稿归属、无进行中任务、有已完成校次、未到最高校次，自动递增校次；并发/重复提交只允许一次成功（提示“该书稿已有待处理的下一校次任务”）。
4. 发布页已有书稿改为可搜索书稿选择框，选中后自动显示上一校次、下一校次、上一完成时间、接收外校公司和重要程度（默认继承，可调整重要程度与公司）。
5. 管理员代发布选择目标责任编辑后筛选其书稿，仍需填写代发布原因并写入代理事件与 audit_log。
6. 新增 test/task-next-stage.test.ts（npm run test:next），24 项测试全部通过；同步更新历史/发布测试以适配“需先完成才能继续下一校次”规则；其余测试、lint、build 回归通过。
7. 正式数据库未修改，唯一联调任务保持 COMPLETED。
8. 下一步：由用户在浏览器人工验收“继续发起下一校次”。

---

# 开发记录：校次与工作内容分离已建立

## 时间

2026年8月21日

## 完成内容

1. 校次与工作内容分离为两个独立维度：校次（初审/一校/二校/三校/加校）与本次工作内容（读校/核红/读校且核红）。
2. tasks 表新增 work_type 字段（PROOFREAD/RED_CHECK/PROOFREAD_AND_RED_CHECK，默认 PROOFREAD），通过幂等迁移补充，现有历史任务兼容默认“读校”。
3. “核红”不再是校次，不参与下一校次自动计算；下一校次顺序调整为 初审→一校→二校→三校→加校，加校完成后允许继续发起加校。
4. 发布页面（新书稿与已有书稿）增加“本次工作内容”三选项（默认读校）；已有书稿的下一校次仍由服务端自动计算且只读。
5. 首页仓库/生产线/已完成、搜索、详情、历史与发布成功结果均同时显示校次与工作内容。
6. 管理员代发布在 audit_log 中记录目标责任编辑、校次、工作内容与代发布原因。
7. 新增 test/work-type.test.ts（npm run test:work），17 项测试全部通过；更新 test:next 以移除“核红为最高校次”旧规则；其余测试、lint、build 回归通过。
8. 正式数据库迁移后数据完整保留（3 条已完成任务，work_type 默认读校）。
9. 下一步：由用户在浏览器人工验收“校次与工作内容分离”。

---

# 开发记录：真实滞留预警与任务排序已建立

## 时间

2026年8月22日

## 完成内容

1. 滞留判断统一：仅判断 PENDING_CONFIRMATION / READY_TO_START / IN_PROGRESS 三类活动任务，COMPLETED、CANCELLED 不计入；计时统一从 published_at 起算，不以外校确认或开始时间重算。
2. 滞留阈值：初审统一 90 天；非初审一星 30 天、二星 15 天、三星 7 天；已等待天数严格大于阈值才算滞留（等于不算）。
3. 首页仓库/仓库页统一排序：三星→二星→一星，同星级按发布时间从早到晚，时间相同按 task.id 升序，由服务端查询完成。
4. 总控预警排序：超出天数从多到少，超出相同按星级降序，再按发布时间从早到晚，最后按 task.id 升序；预警区最多显示 20 条，统计卡展示总数。
5. 预警行显示星级、书名、校次、工作内容、所在环节、已等待天数、阈值、超出天数与“查看历史”链接；滞留任务同时保留在正常仓库/生产线区域。
6. 新增 test/overdue.test.ts（npm run test:overdue），19 项测试全部通过；其余测试、lint、build 回归通过。
7. 正式数据库未修改，4 条已完成任务与历史事件完整保留。

---

# 开发记录：删除与取消保护已建立

## 时间

2026年8月22日

## 完成内容

1. 取消任务功能已建立：仅 INTERNAL_ADMIN 可取消 PENDING_CONFIRMATION 或 READY_TO_START 任务，取消即代替删除，不物理删除任何数据。
2. 完善 lib/task-service.ts 的 cancelTask：取消原因去除首尾空格、必填、最多 200 字；原子更新状态、自动填写 cancelled_by/cancelled_at/cancellation_reason，追加 TASK_CANCELLED 事件与 audit_log。
3. 首页书稿仓库与 /tasks/warehouse 对可取消任务为管理员显示“取消任务”按钮，展开确认面板（书名/校次/工作内容/状态/责任编辑/接收外校公司 + 取消原因 + 返回/确认取消任务），不使用浏览器原生 confirm。
4. 生产代码无任何 DELETE SQL；task_events、audit_log 只追加，books/tasks 无删除入口；账号“停用”仍为可恢复操作，未改动账号管理页面。
5. 新增 test/cancel.test.ts（npm run test:cancel），17 项测试全部通过；其余测试、lint、build 回归通过。
6. 正式数据库未修改，4 条任务与历史事件完整保留。
7. 下一步：由用户在浏览器人工验收“取消任务”。

---

# 开发记录：取消权限扩展与 Dominance 命名

## 时间

2026年8月22日

## 完成内容

1. 取消任务权限扩展：RESPONSIBLE_EDITOR 可取消自己书稿（books.editor_id 等于当前用户）的 PENDING_CONFIRMATION 或 READY_TO_START 任务，不能取消其他责任编辑的任务；INTERNAL_ADMIN（界面名 Dominance）仍可取消任意责任编辑的待确认/待开始任务。
2. 外校主管、校对人员仍不能取消；进行中、已结束、已取消任务不可取消；取消原因必填、去首尾空格、最多 200 字。
3. 责任编辑本人取消时 TASK_CANCELLED 的 is_proxy=0、不写 audit_log；Dominance 代取消时 is_proxy=1、proxy_role=RESPONSIBLE_EDITOR 并写 PROXY_CANCEL 审计。
4. 首页书稿仓库与 /tasks/warehouse 对责任编辑本人书稿的可取消任务也显示“取消任务”按钮（此前仅管理员可见）。
5. INTERNAL_ADMIN 界面显示名统一改为 “Dominance”（内部枚举与数据库值仍为 INTERNAL_ADMIN，账号数据未迁移）。
6. 扩展 test/cancel.test.ts 至 26 项全部通过；其余测试、lint、build 回归通过。
7. 正式数据库未修改（现有任务与历史事件完整保留）。

---

# 开发记录：站内待办与角色提醒已建立

## 时间

2026年8月22日

## 完成内容

1. 站内待办查询服务已建立，集中在 lib/todo-service.ts：只读，从真实 tasks/books/users/companies 实时生成各角色的“我的待办”，不引入演示数据。
2. 待办四分组：urgent（活动任务已超过滞留阈值）/ waiting（待确认收稿、待开始未超时）/ in_progress（进行中未超时）/ completed（最近完成）；activeCount 仅统计前三组，completedCount 单独统计。
3. 角色过滤在服务端完成：INTERNAL_ADMIN 看全部未取消任务；RESPONSIBLE_EDITOR 只看自己书稿（books.editor_id）的任务；EXTERNAL_SUPERVISOR 只看接收外校公司等于本公司的任务；PROOFREADER 看本公司待开始任务与本人进行中/已完成任务。
4. 行动提示按角色与状态生成：外校主管待确认显示“请确认收稿”；校对人员待开始显示“可开始校对”，若本人已有进行中任务则提示“你已有正在校对的任务，请先完成当前任务”；滞留任务显示已滞留天数。
5. 我的待办页已建立（/tasks/my-todos），所有已登录账号可访问；页面为服务端组件，通过 requireCurrentUser 回查数据库真实账号（存在、启用、session_version 一致）取得角色与公司，不信任浏览器传入的角色或用户信息。
6. 首页顶部增加“我的待办（N）”入口，N 为当前用户 activeCount；各分组内按星级降序、等待天数降序、发布时间、task.id 稳定排序。
7. 新增 test/todo.test.ts（npm run test:todo），17 项测试全部通过；全量 327 项测试、npm run lint、npm run build 回归通过。
8. 正式数据库未被测试污染（companies=2、users=5、books=3、tasks=10、task_events=30、audit_log=14），完整性检查 ok。
9. 下一步：由用户分别以责任编辑、外校主管、校对人员、超级管理员登录，人工验收待办范围、分组与数量提示。

---

# 开发记录：我的待办确认收稿入口已建立

## 时间

2026年8月22日

## 完成内容

1. 我的待办页新增外校主管一键“确认收稿”按钮：对状态为 PENDING_CONFIRMATION、且接收外校公司等于当前主管所在公司的任务，右侧显示醒目按钮，替换原来的纯文字“请确认收稿”。
2. 点击立即执行：无二次确认弹窗、无输入框、无确认时间/操作人/公司填写；提交期间按钮显示“处理中…”并禁用防连续点击；成功后提示“已确认收稿”，任务即时从待办消失、页面待办总数减 1、首页待确认收稿数量同步减 1，无须手动刷新。
3. 复用现有 confirmReceiptAction（Server Action）：服务端回查会话真实账号（存在、启用、已完成首次改密、session_version 一致），浏览器不传入操作人、操作时间、操作公司或新状态；确认规则复用 lib/task-service.ts 的 confirmReceipt，未在页面层重复实现另一套规则。
4. 收紧外校主管待办范围：EXTERNAL_SUPERVISOR 的“我的待办”仅含本公司 PENDING_CONFIRMATION 任务，确认后进入书稿仓库即不再是其待办（activeCount 相应减少）。
5. 保留“查看历史”按钮行为不变；发布、开始、结束、取消任务的既有业务规则与 Dominance/责任编辑/校对人员权限均未改动。
6. 新增 test/todo-confirm.test.ts（npm run test:todo-confirm），13 项测试全部通过；全量 340 项测试、npm run lint、npm run build、git diff --check 回归通过。
7. 正式数据库未被修改（integrity_check 与 quick_check 均 ok；companies=2、users=5、books=3、tasks=10、task_events=30、audit_log=14）。
8. 下一步：由外校主管登录“我的待办”点击“确认收稿”进行人工验收。

---

# 开发记录：基本筛选已建立

## 时间

2026年8月22日

## 完成内容

1. 基本筛选功能已建立：新增 /tasks 任务筛选页，支持按责任编辑、校对负责人、校次、状态四个维度筛选，多条件 AND 组合，全部登录账号可只读查看（开放查看原则）。
2. 新增 lib/task-filter-service.ts 只读查询服务：filterTasks 全部参数绑定、无 SQL 注入；parseTaskFilter 校验并忽略非法值（非正整数、未知校次/状态一律视为不筛选）；listFilterOptions 从真实 users 生成下拉选项，责任编辑、校对负责人含已停用人员，保证其历史书稿仍可被筛选到。
3. 校次选项为 5 个有效校次（初审/一校/二校/三校/加校，复用 task-service 的 STAGES），状态选项覆盖全部 5 个状态（含已取消）；结果按发布时间倒序、id 倒序稳定排序。
4. 页面为服务端组件，通过 requireCurrentUser 回查真实账号（存在、启用、session_version 一致），不信任浏览器传入角色；筛选经 GET 参数提交、纯 HTML form，无客户端脚本、无 useSearchParams。
5. 首页顶部增加“任务筛选”入口；复用 DashboardTaskRow 展示，与首页/仓库/搜索/详情保持一致（星级/校次/工作内容/状态/查看历史）。
6. 本阶段为只读功能，不修改任务状态、不追加事件、不写审计日志；四类角色权限与发布/确认/开始/结束/取消等既有规则均未改动。
7. 新增 test/task-filter.test.ts（npm run test:filter），13 项测试全部通过；全量 353 项测试、npm run lint、npm run build、git diff --check 回归通过。
8. 本阶段为只读功能，代码与自动测试均未修改正式数据库（integrity_check 与 quick_check 均 ok；companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14，其中较上一阶段新增的 1 条待确认任务与 1 条 TASK_PUBLISHED 事件来自人工验收期间的浏览器操作，非本阶段代码或测试写入）。
9. 下一步：由用户以不同角色登录，人工验收单独/组合筛选与只读查看。

---

# 开发记录：数据库备份与恢复验证已建立

## 时间

2026年8月22日

## 完成内容

1. 新增 scripts/db-backup.mjs：使用 better-sqlite3 的 db.backup() 在线备份 API，兼容 WAL 模式；默认备份目录 data/backups/，文件名 publishing-process-<YYYYMMDD>-<HHMMSS>.db，自动建目录、同一秒重复执行自动加序号、禁止备份目标等于正式库路径、正式库不存在时安全退出。
2. 备份完成后以只读方式打开备份库，执行 integrity_check、quick_check 与六张表行数统计，并与备份前读取的正式库行数比对，全部一致才报告“备份成功”；全程只读打开正式库，不修改任何业务数据，不输出密码、哈希或 AUTH_SECRET。
3. 新增 scripts/db-restore-check.mjs：必须显式传入备份文件路径，拒绝操作正式数据库路径；在系统临时目录创建独立临时文件夹，复制备份到临时库后只读校验完整性与六表行数，并与原备份库比对，全部一致才提示“恢复验证通过”；验证结束只删除临时副本，不删除原备份文件、不触碰正式库。
4. package.json 新增 npm run db:backup 与 npm run db:restore-check -- <备份文件路径>；.gitignore 新增 /data/backups/，备份文件不进入 Git，原有 *.db / *.db-wal / *.db-shm / *.db-journal 规则不变。
5. 新增 docs/08-部署与备份说明.md：说明正式库位置、备份/恢复命令、命名规则、临时目录恢复验证、WAL 不能直接复制的原因、GitHub 不能代替数据备份、本地备份不能防磁盘损坏、异地存储、备份频率与保留、真正恢复前停止服务并保留故障库副本。
6. 已实际执行一次备份（data/backups/publishing-process-20260822-150650.db，81920 字节）并对其执行一次恢复验证，备份库与临时恢复库完整性均 ok，六表行数与正式库一致。
7. 全量 353 项测试、npm run lint、npm run build、git diff --check 回归通过；正式数据库六表行数前后不变（companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14），integrity_check 与 quick_check 均 ok。
8. 本阶段未提交 Git、未推送 GitHub；备份文件已被 Git 忽略，未进入版本控制。

---

# 开发记录：待确认收稿与书稿仓库归类分离修复

## 时间

2026年8月22日

## 完成内容

1. 业务归类调整：PENDING_CONFIRMATION 仅进入「待确认收稿」专页与对应角色待办，不再进入首页书稿仓库与完整仓库页；书稿仓库仅含 READY_TO_START。
2. lib/dashboard-service.ts 的 listWarehouse、countWarehouse 改为仅 READY_TO_START；locationLabel 待确认环节显示名改为「待确认收稿」。
3. 首页与 /tasks/warehouse 的书稿仓库只查询/显示 READY_TO_START，空状态文案与取消条件同步调整。
4. /tasks/pending-confirmation 待确认专页复用 CancelActions 为责任编辑本人书稿与 Dominance 增加「取消任务」入口，取消权限与业务逻辑复用 cancelTask，未另写一套。
5. lib/task-service.ts 的 listPendingConfirmation 补充 editorId、work_type 字段，供取消面板展示校次/工作内容/责任编辑。
6. 更新 dashboard.test.ts、overdue.test.ts 及 scripts/db-acceptance.ts 中仓库归类断言；更新 docs/01、docs/04、docs/05 的归类定义。
7. 全量 353 项测试、npm run lint、npm run build、git diff --check 回归通过；自动验收脚本 52 项通过；正式数据库未修改（companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14），integrity_check 与 quick_check 均 ok。
8. 下一步：由用户在浏览器复验待确认专页的确认与取消入口及仓库归类。

---

# 开发记录：校对人员待办与搜索结果开始修正

## 时间

2026年8月22日

## 完成内容

1. 校对人员「我的待办」改为只查询本人 IN_PROGRESS 任务（最多 1 条），不显示仓库 READY_TO_START 任务池与最近完成；activeCount 只能为 0 或 1。
2. 校对人员空闲时待办页显示「当前没有进行中的校对任务」，本人进行中任务继续显示「查看历史」；本人任务滞留时仍计入 urgent 分组突出显示，仍只算 1 条。
3. 搜索结果允许校对人员开始任务：服务端回查真实账号/角色/公司，对「本人公司 + READY_TO_START + 无进行中」显示「开始校对」，复用现有 startTaskAction/startTask，不另写业务逻辑；已有进行中时显示「你已有正在校对的任务，请先完成当前任务」。
4. 责任编辑、外校主管、Dominance 在搜索结果不显示开始按钮；非本公司 READY_TO_START 只允许查看历史；并发与一人一书约束继续由 startTask 原子更新与唯一索引保证。
5. lib/search-service.ts 搜索结果补充 companyId，新增 proofreaderStartDecision、hasInProgressTask；新增 components/SearchStartActions.tsx。
6. 更新 test/todo.test.ts 并新增 test/search-start.test.ts（10 项）；全量 363 项测试、npm run lint、npm run build、git diff --check 回归通过。
7. 正式数据库未修改（companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14），integrity_check 与 quick_check 均 ok。
8. 下一步：由用户复验校对人员待办与搜索结果开始入口。

---

# 开发记录：首页待确认收稿入口按角色显示

## 时间

2026年8月22日

## 完成内容

1. 首页顶部「待确认收稿（N）」入口及数量仅对 PROOFREADER 隐藏；EXTERNAL_SUPERVISOR、RESPONSIBLE_EDITOR、INTERNAL_ADMIN 继续显示。
2. 不修改待确认收稿页面、确认权限或任务状态；开放查看原则不变，校对人员仍可通过搜索与任务筛选查看书稿。
3. test/dashboard.test.ts 新增角色导航显示测试；全量 364 项测试、npm run lint、npm run build、git diff --check 回归通过。
4. 正式数据库未修改（companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14），integrity_check 与 quick_check 均 ok。

---

# 开发记录：外校主管首页优化

## 时间

2026年8月22日

## 完成内容

1. 首页为 EXTERNAL_SUPERVISOR 增加「待确认收稿」展开区域（统计卡后、书稿仓库前），仅显示本公司 PENDING_CONFIRMATION，按星级降序、同星级来稿时间升序；每条含星级/书名/校次/工作内容/责任编辑/发布单位/来稿时间/已等待天数/查看历史/确认收稿；最多 20 条，超出显示「查看全部待确认收稿」。
2. 确认收稿复用现有 confirmReceiptAction/confirmReceipt，点击防连续点击，成功后刷新并即时进入书稿仓库；不重写业务逻辑、不改状态定义。
3. 外校主管顶部隐藏「待确认收稿」「我的待办」入口，保留任务筛选/姓名角色/退出与搜索框。
4. 外校主管首页不展开「已完成」长列表，「已完成任务」统计卡可点击，进入 /tasks/completed 独立分页页面（默认仅本公司 COMPLETED，按 finished_at 倒序，每页 20 条，含返回首页与总数）。
5. 其余三类角色首页与导航不变；开放查看原则不变（外校主管仍可通过搜索与任务筛选查看其他书稿）。
6. lib/dashboard-service.ts 新增 listCompletedPage；lib/task-service.ts listPendingConfirmation 补充 bookId；新增 components/SupervisorPendingList.tsx、app/tasks/completed/page.tsx。
7. 新增 test/supervisor-home.test.ts（11 项）；全量 375 项测试、npm run lint、npm run build、git diff --check 回归通过。
8. 正式数据库未修改（companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14），integrity_check 与 quick_check 均 ok。

---

# 开发记录：责任编辑首页角色化

## 时间

2026年8月22日

## 完成内容

1. 责任编辑首页按本人维度展示：统计卡改为 我的现有书稿/我的待确认/部门书稿仓库/我的生产线/我的已完成；待确认、生产线、已完成均仅统计本人书稿（books.editor_id = 当前用户）。
2. 部门书稿仓库展示全部 READY_TO_START（不按责任编辑限制），可按接收外校公司筛选（GET 参数 company），并显示各公司当前仓库数量；责任编辑在仓库中只读（不开始、不确认、不修改他人任务，仅本人可取消）。
3. 不展示全部门生产线、滞留统计卡与总控预警；本人任务滞留保留行内标记，不建独立预警区。
4. 导航「待确认收稿（N）」对责任编辑改为显示本人待确认数量。
5. lib/dashboard-service.ts 新增 listProductionByEditor、listCompletedByEditor、countActiveBooksByEditor、countWarehouseByCompany；首页与 /tasks/warehouse 支持按外校公司筛选。
6. 未来扩展：多家外校公司时责任编辑可按各公司仓库数量判断承载并选择接收公司；当前仅提供仓库可见、公司名称、数量与筛选。
7. 新增 test/editor-home.test.ts（10 项）；全量 385 项测试、npm run lint、npm run build、git diff --check 回归通过。
8. 正式数据库未修改（companies=2、users=5、books=3、tasks=11、task_events=32、audit_log=14），integrity_check 与 quick_check 均 ok。

---

# 开发记录：第9天完整验收完成

## 时间

2026年8月22日

## 完成内容

1. 第9天人工验收全部通过：四步业务闭环、一人一书、操作历史、下一校次、待确认/仓库分离、三类角色首页（校对人员/外校主管/责任编辑）、搜索与筛选及开放查看均复验正常。
2. 人工验收期间发现的 7 项问题已全部修复并复验通过（待确认误入仓库、校对人员待办重复、搜索缺少开始、校对人员待确认入口、外校主管确认入口、外校主管已完成列表、责任编辑首页范围）。
3. 最终状态归类统一：待确认收稿=PENDING_CONFIRMATION、书稿仓库=READY_TO_START、生产线=IN_PROGRESS、已完成=COMPLETED；核红是工作内容（work_type），不是校次（stage）。
4. 全量自动测试、lint、build、git diff --check 均通过；正式数据库未修改，临时验收数据库与备份暂保留至第10天结束。
